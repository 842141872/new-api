# 新功能说明文档

## 功能列表（简明版 - 测试指南）

### 日志管理增强（8项）

**1. 重试渠道名称显示**
- 在使用日志的"重试"列，鼠标悬停可显示完整的重试渠道名称列表（带颜色标签）
- 测试方法：查看有重试记录的日志，鼠标悬停"重试"列

**2. 模型名称模糊搜索**
- 日志筛选支持模糊匹配模型名称
- 测试方法：在日志页面输入部分模型名称（如"gpt"），可搜到所有包含"gpt"的模型

**3. 空回复筛选**
- 新增"空回复筛选"下拉选项，可快速筛选出补全内容为空的请求
- 三个选项：全部 / 仅空回复 / 仅非空回复
- 测试方法：在日志页面选择"仅空回复"，查看是否只显示返回内容为空的请求

**4. 多渠道ID筛选**
- 渠道ID筛选支持同时搜索多个渠道
- 单个ID：直接输入点击查询 / 多个ID：输入后回车添加标签
- 测试方法：输入渠道ID如"101"按回车，再输入"102"按回车，点击查询

**5. 输入/输出字数筛选**
- 新增"输入/输出字数"筛选框，按tokens数量精确查找
- 输入数字后自动搜索输入或输出tokens等于该数字的所有日志
- 测试方法：输入"1000"点击查询，查看结果是否包含输入=1000或输出=1000的记录

**6. 渠道管理日志快捷跳转**
- 渠道管理界面新增"日志"按钮，点击后自动跳转到该渠道的使用日志
- 测试方法：进入渠道管理，点击任意渠道的"日志"按钮，检查是否自动跳转并填充渠道ID

**7. 重试渠道名称修复**
- 即使渠道被删除，使用日志也能显示原始渠道名称（不再显示"渠道X"）
- 测试方法：删除一个有使用记录的渠道，查看历史日志是否仍显示渠道名称

**8. 错误日志模型重定向显示**
- 失败的请求也能看到模型重定向信息（与成功日志一致）
- 测试方法：查看错误日志(type=5)，鼠标悬停模型列，检查是否显示重定向信息

---

### 用户管理增强（2项）

**9. 邀请人搜索**
- 用户管理界面新增"邀请人搜索"输入框
- 支持通过邀请人的用户ID、用户名或邮箱搜索被邀请的所有用户
- 测试方法：输入某个用户的ID或用户名，查看是否显示该用户邀请的所有下级用户

**10. 用户注册时间记录**
- 系统自动记录用户注册时间，在用户管理界面显示
- 老用户显示灰色"未记录"标签
- 测试方法：创建新用户，检查"注册时间"列是否显示当前时间

---

### 界面体验优化（3项）

**11. 模型广场默认表格视图**
- 进入模型广场（价格页面）时，默认显示表格视图而非卡片视图
- 测试方法：进入价格页面，检查是否默认为表格模式

**12. 管理员默认100条/页**
- 管理员角色（role>=10）所有表格默认每页显示100条数据
- 普通用户保持10条/页
- 测试方法：用管理员账号登录，检查各表格默认是否为100条/页

**13. 页码选项扩展**
- 所有表格的每页条数选项扩展为：10/20/50/100/200/500/1000
- 测试方法：打开任意表格的每页条数下拉框，检查是否有7个选项

---

### 功能增强（7项）

**14. 错误响应替换**
- 自动将API技术错误替换为友好的自定义提示
- 需在 `.env` 配置 `ERROR_RESPONSE_REPLACE_ENABLED=true`
- 测试方法：配置错误映射后，触发对应错误，检查是否显示自定义消息

**15. 兑换码批量生成**
- 兑换码批量生成上限从100提升到1000个
- 测试方法：在兑换码管理页面尝试生成1000个兑换码

**16. 渠道搜索增强**
- 渠道搜索支持 model_mapping（模型映射）字段搜索
- 测试方法：在渠道管理页面搜索模型映射中的模型名称

**17. 兑换码key搜索**
- 支持直接按兑换码key进行搜索
- 测试方法：在兑换码管理页面输入完整的兑换码key，检查是否能精确找到

**18. 模型重定向编辑器优化**
- 渠道模型重定向可视化编辑模式，左右输入框宽度统一为1200px
- 较长的模型名称能够完整显示
- 测试方法：编辑渠道的模型重定向，切换到可视化模式，检查左右输入框宽度是否一致

**19. 禁用额度不足邮件提醒** ⭐ NEW
- 临时禁用额度不足时的所有通知（邮件/Webhook/Bark/Gotify）
- 不影响额度扣除、日志记录等核心功能
- 降低邮件发送成本
- 测试方法：用户额度低于阈值时不会收到任何通知

**20. Gemini 空响应错误提示优化** ⭐ NEW
- 当 Gemini API 返回空 candidates 时，显示友好的中文错误提示
- 错误提示："违反谷歌政策空回啦，建议换preset哦~"
- 如果有具体原因会显示："违反谷歌政策空回啦，建议换preset哦~(原因: SAFETY)"
- 解决了之前静默返回空响应的问题，让用户明确知道是 Google 政策限制
- 测试方法：使用受限的 Gemini key 发送请求，会看到中文错误提示而非空回

**21. QQ邮箱注册验证** ⭐ NEW
- 限制QQ邮箱注册时，邮箱账号必须为纯数字格式
- 前后端双重验证，确保数据一致性
- 测试方法：尝试使用非纯数字的QQ邮箱注册（如 abc123@qq.com），会提示"QQ邮箱仅支持纯数字账号（例如：123456789@qq.com）"

**22. 兑换码功能浏览器兼容性修复** ⭐ NEW
- 修复壹钱包等平台复制兑换码时带空格导致兑换失败的问题
- 修复快速点击或网络慢时重复提交导致提示"已被使用"的问题
- 修复请求头类型不兼容某些浏览器的问题
- 修复错误处理空指针导致某些浏览器崩溃的问题
- 测试方法：复制带空格的兑换码测试，或快速双击兑换按钮测试

---

## 新增功能（详细版）

### 1. 错误响应替换功能
当API返回包含特定关键词的错误时，自动替换为自定义错误消息。支持通过JSON键值对配置多个错误映射，每个关键词可以对应不同的自定义提示，完全替换原始错误内容。
- **配置方式**：在 `.env` 配置 `ERROR_RESPONSE_REPLACE_ENABLED=true` 启用
- **映射配置**：通过 `ERROR_RESPONSE_MAPPING` 配置JSON格式的错误映射关系
- **匹配规则**：支持模糊匹配，只要错误消息包含关键词即可触发替换
- **替换效果**：完全替换为自定义消息，原始错误不会显示给用户
- **配置示例**：
  ```bash
  ERROR_RESPONSE_REPLACE_ENABLED=true
  ERROR_RESPONSE_MAPPING='{"no can":"当前模型繁忙，请稍后重试","empty":"模型返回为空","PROHIBITED":"内容违反政策"}'
  ```
- **默认配置**：
  ```json
  {
    "no candidates returned": "当前模型繁忙，请稍后重试",
    "empty response": "模型返回为空，请检查输入内容",
    "PROHIBITED_CONTENT": "内容违反使用政策，请修改后重试"
  }
  ```

### 2. 使用日志重试渠道名称显示
在使用日志的"重试"列中，鼠标悬停可显示完整的重试渠道名称列表（带颜色标签）。原来只显示"渠道：1->2->3"，现在悬停可看到每个渠道的实际名称。

### 3. 兑换码批量生成限制提升
兑换码批量生成上限从100提升到1000个。

### 4. 渠道搜索增强
渠道搜索支持 model_mapping 字段搜索。

### 5. 日志模型名称模糊搜索
日志筛选支持模糊匹配模型名称。

### 6. 兑换码按key搜索
支持直接按兑换码key进行搜索。

### 7. 模型广场默认表格视图
进入模型广场（价格页面）时，默认显示为表格视图而非卡片视图,方便快速浏览大量模型信息。

### 8. 管理员页面每页显示100条
当用户角色为管理员或超级管理员（role >= 10）时，所有表格的默认每页条数自动设置为100条，提升浏览效率。普通用户保持10条/页。

### 9. 页码选项扩展
所有表格的每页条数选项从原来的 `[10, 20, 50, 100]` 扩展为 `[10, 20, 50, 100, 200, 500, 1000]`，支持更大批量的数据浏览。
- **前端修改**：统一使用 `constants/common.constant.js` 中定义的 `PAGE_SIZE_OPTIONS`
- **后端限制**：`common/page_info.go` 将 page_size 上限从100提升到1000
- **修改文件**（前端，共9个）：
  - `components/table/usage-logs/UsageLogsTable.jsx`
  - `components/table/redemptions/RedemptionsTable.jsx`
  - `components/table/task-logs/TaskLogsTable.jsx`
  - `components/table/mj-logs/MjLogsTable.jsx`
  - `components/table/model-pricing/view/table/PricingTable.jsx`
  - `components/table/model-pricing/view/card/PricingCardView.jsx`
  - `components/table/tokens/TokensTable.jsx`
  - `components/table/models/ModelsTable.jsx`
  - `pages/Setting/Ratio/ModelRationNotSetEditor.jsx`
- **修改文件**（后端）：`common/page_info.go:77-78`

### 10. 用户邀请人搜索功能
在用户管理界面添加邀请人搜索输入框，支持通过邀请人的用户ID、用户名或邮箱地址搜索该邀请人所邀请的所有用户。搜索优先级：邀请人搜索 > 关键词搜索 > 分组筛选。
- 新增API端点：`GET /api/user/search_by_inviter`
- 前端组件增强：UsersFilters 添加独立的邀请人搜索框
- 支持分页查询，返回格式与现有用户查询接口保持一致

### 11. 用户注册时间记录功能
系统自动记录用户注册时间（精确到秒），在用户管理界面的"注册时间"列显示。采用 GORM 标准 `CreatedAt` 字段实现，确保与未来官方代码合并时的最低冲突风险。
- **后端实现**：User 模型添加 `CreatedAt time.Time` 字段（GORM 标准字段，自动管理）
- **前端展示**：UsersColumnDefs 新增"注册时间"列，支持本地化时间格式显示（格式：2025-01-12 15:30:45）
- **零值处理**：老用户（注册时间为零值 `0001-01-01T00:00:00Z`）显示灰色"未记录"标签
- **数据库兼容**：GORM AutoMigrate 自动添加字段，支持 MySQL/PostgreSQL/SQLite，无需手动迁移
- **合并安全性**：使用 GORM 官方标准字段，与上游代码合并时冲突风险最低

### 12. 渠道管理日志快捷跳转功能
在渠道管理界面的操作列新增"日志"按钮，点击后自动跳转到使用日志页面并预填该渠道ID，同时自动触发搜索，方便管理员快速查看特定渠道的使用记录。
- **前端实现**：ChannelsColumnDefs 操作列新增"日志"按钮（位于"编辑"按钮之后）
- **导航方式**：使用 React Router URL 参数传递 (`/log?channel=渠道ID`)
- **自动填充**：usage-logs/index.jsx 使用 useEffect 监听 URL 参数，自动填入渠道ID到筛选表单
- **自动搜索**：填充渠道ID后自动调用 `logsData.refresh()` 触发搜索
- **用户体验**：URL 参数方式支持分享链接和刷新保持状态

### 13. 模型重定向可视化编辑器布局优化
优化渠道管理中模型重定向（Model Redirect）可视化编辑模式的键值对布局，将左右两侧输入框宽度从原来的 600px 统一调整为 1200px，使两侧宽度一致，确保较长的模型名称能够完整显示。
- **修改文件**：`web/src/components/common/ui/JSONEditor.jsx`
- **修改位置**：第 446、473 行，`renderKeyValueEditor` 函数中的输入框容器宽度
- **改进效果**：左右两侧输入框宽度相等（各 1200px），删除按钮固定尺寸（24x24px）
- **布局方式**：使用 flex 布局（`display: 'flex', gap: 8, alignItems: 'center'`）
- **影响范围**：所有使用 JSONEditor 组件的 keyValue 编辑类型（包括模型重定向、状态码复写等）

### 14. 修复重试渠道名称显示问题
修复了当重试渠道失败或被删除时，使用日志中不显示渠道原名称而是显示"渠道ID"的问题。现在系统会在重试时实时保存渠道名称，即使渠道后来被删除，也能正确显示原始名称。
- **后端实现**：在 `addUsedChannel` 函数中同时保存渠道ID和名称到gin context
- **数据保存**：`use_channel_names` 数组与 `use_channel` 数组对应，保存各重试渠道的名称
- **兼容性**：优先使用保存的渠道名称，回退到数据库查询（兼容旧数据）
- **修改文件**：
  - `controller/relay.go`: 修改 `addUsedChannel` 函数和初始化逻辑
  - `service/log_info_generate.go`: 在日志信息生成时包含渠道名称
  - `model/log.go`: 优先使用保存的渠道名称，避免"渠道X"显示问题

### 15. 修复错误日志模型重定向显示问题
修复了错误日志（type=5）不显示模型重定向信息的问题。现在当请求失败时，如果存在模型重定向，错误日志也会显示原始请求模型和实际重定向模型的信息，与成功日志保持一致的用户体验。
- **问题描述**：之前只有成功的请求日志会显示模型重定向信息，失败的请求看不到重定向详情
- **修复方案**：在错误日志记录时同样包含 `is_model_mapped` 和 `upstream_model_name` 字段
- **用户体验**：鼠标悬停错误日志的模型列，也能看到"请求并计费模型"和"实际模型"的对比信息
- **修改文件**：`controller/relay.go` 的 `processChannelError` 函数（320-324行）

### 16. 使用日志空回复筛选功能
在使用日志页面新增"空回复筛选"下拉选项，可以快速筛选出补全内容为空的请求日志（completion_tokens = 0），并自动排除错误日志（type=5），帮助管理员快速发现和分析返回空内容但未报错的API调用。
- **功能说明**：
  - **全部**：显示所有日志，不进行筛选
  - **仅空回复**：只显示成功但返回为空的请求（`completion_tokens = 0` 且 `type != 5`）
  - **仅非空回复**：只显示有返回内容的请求（`completion_tokens > 0`）
- **前端实现**：
  - `UsageLogsFilters.jsx` (89-99行)：添加"空回复筛选"下拉框
    - 三个选项：「全部」、「仅空回复」、「仅非空回复」
    - 使用 `Form.Select` 组件，`pure` 和 `size='small'` 样式
  - `useUsageLogsData.jsx`：
    - 表单初始值添加 `empty_response: 'all'`（91-97行）
    - `getFormValues()` 函数传递 `empty_response` 参数（238行）
    - `loadLogs()` 函数URL包含 `empty_response` 查询参数（501、503行）
    - `getLogStat()` 和 `getLogSelfStat()` 统计函数同样支持该筛选（245、271行）
  - `en.json`: 添加国际化翻译支持
- **后端实现**：
  - `controller/log.go`：
    - `GetAllLogs()` 函数接收 `empty_response` 查询参数（23行）
    - `GetUserLogs()` 函数接收 `empty_response` 查询参数（44行）
    - 参数传递给 `model` 层进行数据库查询
  - `model/log.go`：
    - `GetAllLogs()` 函数筛选逻辑（234-240行）：
      - `empty_response=empty`: `(completion_tokens = 0 OR completion_tokens IS NULL) AND type != 5`
      - `empty_response=non_empty`: `completion_tokens > 0`
      - 其他值：不进行筛选
    - `GetUserLogs()` 函数筛选逻辑（365-371行）：与 `GetAllLogs()` 相同
- **核心逻辑优化**：
  - 空回复筛选**自动排除错误日志**（type=5），因为错误日志必然为空回复
  - 只筛选**成功但返回为空**的请求，帮助发现配置问题或模型异常
  - 使用 `IS NULL` 兼容老数据中 `completion_tokens` 字段可能为空的情况
- **应用场景**：
  - 快速定位返回空内容但未报错的异常请求（可能是模型繁忙、内容过滤等）
  - 分析模型调用成功但输出为空的情况（与报错区分开）
  - 优化API使用，减少无效调用和浪费的配额
  - 检查特定模型或渠道的空回复率

### 17. 使用日志多渠道ID筛选功能
在使用日志页面的渠道ID筛选支持同时搜索多个渠道，支持两种使用方式：单个ID直接搜索（无需回车）和多个ID标签搜索（回车添加标签）。
- **使用方式**：
  - **单个渠道ID**：输入渠道ID（如 `101`），不按回车直接点击「查询」按钮，正常搜索该渠道
  - **多个渠道ID**：输入渠道ID（如 `101`）按回车添加为标签，继续输入下一个ID（如 `102`）按回车，可同时搜索多个渠道
  - 标签右上角的 × 可以删除已添加的渠道ID
- **前端实现**：
  - `UsageLogsFilters.jsx` (103-111行)：将 `Form.Input` 替换为 `Form.TagInput` 组件
    - 提示文字：「渠道 ID（支持多个，回车添加）」
    - 配置 `separator={null}` 和 `addOnBlur={false}` 确保只能按回车添加标签
  - `useUsageLogsData.jsx`：
    - 初始值从 `channel: ''` 改为 `channel: []`（91-97行）
    - `getFormValues()` 函数智能处理渠道值（218-228行）：
      - 如果是字符串（单个ID未按回车）→ 直接使用
      - 如果是数组（多个ID已创建标签）→ 转换为逗号分隔字符串
- **后端实现**：
  - `controller/log.go`：
    - 新增 `parseChannelIDs()` 解析函数（13-40行），支持解析逗号分隔或JSON数组格式
    - 更新 `GetAllLogs()`、`GetLogsStat()` 和 `GetLogsSelfStat()` 三个函数，参数从 `channel int` 改为 `channels []int`
  - `model/log.go`：
    - `GetAllLogs()` 函数签名更新（202行）：参数从 `channel int` 改为 `channels []int`
    - SQL查询从 `WHERE channel_id = ?` 改为 `WHERE channel_id IN ?`（225-227行）
    - `SumUsedQuota()` 函数同样更新（402行及426-429行）
- **技术细节**：
  - 前端发送数据格式：`channel=101` 或 `channel=101,102,103`
  - 后端解析逻辑：支持单个数字、逗号分隔字符串、JSON数组格式
  - SQL查询优化：使用 `IN` 子句实现高效的多渠道查询
- **应用场景**：
  - 对比多个渠道的使用情况和性能
  - 批量查看特定渠道组的日志记录
  - 快速排查多个渠道的异常问题

### 18. 使用日志按输入/输出字数精确筛选功能
在使用日志页面新增"输入/输出字数"筛选框，可以按输入tokens（prompt_tokens）或输出tokens（completion_tokens）进行精确匹配筛选，使用OR逻辑（满足任一条件即可）。
- **功能说明**：
  - 输入数字后点击「查询」，自动搜索输入tokens=该数字 **或** 输出tokens=该数字的所有日志
  - 例如：输入 `1000`，会找出所有 `prompt_tokens=1000` 或 `completion_tokens=1000` 的记录
  - **OR逻辑**：只要输入或输出任一字段匹配即可，不需要同时匹配
  - **精确匹配**：不是范围筛选，而是完全相等匹配（`=`操作符）
- **前端实现**：
  - `UsageLogsFilters.jsx` (96-105行)：添加 `Form.InputNumber` 组件
    - 字段名：`token_count`
    - 提示文字：「输入/输出字数」
    - 配置：`min={0}`, `hideButtons`, `showClear`
  - `useUsageLogsData.jsx`：
    - 表单初始值添加 `token_count: ''`（91-97行）
    - `getFormValues()` 函数传递 `token_count` 参数（242行）
    - `loadLogs()` 函数URL包含 `token_count` 查询参数（263、291行）
  - `zh.json` 和 `en.json`：
    - 中文：「输入/输出字数」
    - 英文：「Input/Output Tokens」
- **后端实现**：
  - `controller/log.go`：
    - `GetAllLogs()` 函数接收 `token_count` 查询参数（55行）
    - `GetUserLogs()` 函数接收 `token_count` 查询参数（77行）
    - 参数传递给 `model` 层进行数据库查询（56、78行）
  - `model/log.go`：
    - `GetAllLogs()` 函数筛选逻辑（256-258行）：
      ```go
      if tokenCount > 0 {
          tx = tx.Where("logs.prompt_tokens = ? OR logs.completion_tokens = ?", tokenCount, tokenCount)
      }
      ```
    - `GetUserLogs()` 函数筛选逻辑（391-393行）：与 `GetAllLogs()` 相同
- **SQL查询示例**：
  - 输入 `1000` 后的SQL：`WHERE (prompt_tokens = 1000 OR completion_tokens = 1000)`
  - 只使用一个输入框，同时搜索两个字段，避免界面复杂化
- **应用场景**：
  - 快速查找特定tokens数量的请求（例如：找出所有输出1000字的请求）
  - 分析特定tokens消耗的API调用模式
  - 排查异常的tokens使用情况（输入或输出异常大/小的请求）
  - 验证计费准确性（检查特定tokens数的计费记录）

---

## Bug修复与优化

### 1. 修复渠道ID收集bug
修复了 `model/log.go` 中只收集主渠道ID而遗漏重试渠道ID的问题，现在会正确收集所有渠道ID并查询名称。

### 2. 修复前端JSON解析异常
修复了 `UsageLogsColumnDefs.jsx` 中直接使用 `JSON.parse()` 可能导致组件崩溃的问题，改用安全的 `getLogOther()` 函数。
- **修复位置**：`UsageLogsColumnDefs.jsx` 第346行（GROUP列渲染逻辑）
- **修复方法**：将 `JSON.parse(record.other)` 替换为 `getLogOther(record.other)`
- **影响范围**：所有使用日志的分组列显示

### 3. 字符串处理性能优化
将自定义的字符串处理函数（`splitString`、`trimSpace`、`containsString`）替换为Go标准库函数（`strings.Split`、`strings.TrimSpace`、`strings.Contains`），提升性能和可维护性。

### 4. 修复管理员页面默认页码localStorage回退问题
修复了多个表格组件中localStorage回退值使用固定 `ITEMS_PER_PAGE`(10) 而非 `getDefaultPageSize()` 的问题，确保管理员首次访问时能正确显示100条/页。
- **修复文件**：
  - `hooks/channels/useChannelsData.jsx` (line 124)
  - `hooks/usage-logs/useUsageLogsData.jsx` (line 558)
  - `hooks/mj-logs/useMjLogsData.js` (line 282)
  - `hooks/task-logs/useTaskLogsData.js` (line 273)
- **修复前**：`parseInt(localStorage.getItem('page-size')) || ITEMS_PER_PAGE`
- **修复后**：`parseInt(localStorage.getItem('page-size')) || getDefaultPageSize()`
- **影响范围**：管理员首次访问各表格页面时的默认每页条数

---

## 技术实现细节

### 错误响应替换功能技术架构
- **配置存储**：`common/constants.go` 定义 `ErrorResponseMapping map[string]string`
- **配置解析**：`common/init.go` 使用 `json.Unmarshal` 解析环境变量中的JSON字符串
- **替换逻辑**：`types/error.go` 的 `ReplaceErrorMessageIfMatched()` 函数
  - 遍历映射表，使用 `strings.Contains()` 进行模糊匹配
  - 匹配成功后调用 `e.SetMessage(replaceMessage)` 完全替换错误消息
  - 不会在替换后的消息中包含原始错误内容
- **应用位置**：所有错误响应返回给用户前都会经过此函数处理

### 管理员默认页码实现
- **判断函数**：`helpers/utils.jsx` 中的 `getDefaultPageSize()` 函数
  - 调用 `isAdmin()` 检查用户角色（role >= 10）
  - 管理员返回100，普通用户返回10
- **使用位置**：
  - 所有表格Hook的初始state值：`useState(getDefaultPageSize())`
  - localStorage回退值：`localStorage.getItem('xxx-page-size') || getDefaultPageSize()`
- **页码选项**：`constants/common.constant.js` 定义 `PAGE_SIZE_OPTIONS = [10, 20, 50, 100, 200, 500, 1000]`

---

### 19. 禁用额度不足邮件提醒功能
为降低邮件发送成本,临时禁用了额度不足时的所有通知功能(包括邮件、Webhook、Bark、Gotify推送)。此修改不影响任何核心业务功能,所有额度扣除、日志记录、API请求处理等均正常运行。
- **实现方式**:在 `checkAndSendQuotaNotify` 函数开头添加 `return` 语句,函数被调用但立即返回
- **影响范围**:
  - ❌ 禁用功能:额度不足邮件提醒、Webhook通知、Bark推送、Gotify通知
  - ✅ 不受影响:用户额度扣除、Token额度扣除、消费日志记录、API请求处理
- **恢复方法**:删除 `service/quota.go:525` 行的 `return` 语句和相关注释,重新编译即可
- **修改文件**:`service/quota.go` (line 522-573)
- **成本优化**:避免频繁发送额度预警邮件产生的邮件服务费用
- **应用场景**:
  - 邮件服务费用较高的环境
  - 用户量大且额度预警频繁的场景
  - 临时关闭通知功能但保持核心业务运行

### 21. QQ邮箱注册验证功能
限制QQ邮箱注册时必须使用纯数字账号格式，防止用户使用无效的QQ邮箱格式导致后续无法收到验证邮件。前后端双重验证确保数据一致性。

- **功能说明**：
  - QQ邮箱的有效格式为纯数字账号（如：123456789@qq.com）
  - 不支持带字母或其他字符的格式（如：abc123@qq.com、user_name@qq.com）
  - 空账号也会被拒绝（如：@qq.com）

- **前端验证**：
  - **文件位置**：`web/src/components/auth/RegisterForm.jsx` (209-246行)
  - **验证时机**：点击"获取验证码"按钮时（`sendVerificationCode` 函数）
  - **验证逻辑**：
    ```javascript
    // 检查是否为QQ邮箱
    if (inputs.email.toLowerCase().endsWith('@qq.com')) {
      const username = inputs.email.split('@')[0];
      const isNumeric = /^\d+$/.test(username);
      if (!isNumeric || username === '') {
        showError('QQ邮箱仅支持纯数字账号（例如：123456789@qq.com）');
        return;
      }
    }
    ```
  - **用户体验**：在发送验证码前就拦截错误格式，避免浪费验证码配额

- **后端验证**：
  - **文件位置**：`controller/user.go` (184-205行)
  - **验证时机**：用户提交注册表单时（`Register` 函数）
  - **验证逻辑**：
    ```go
    // 验证QQ邮箱格式：如果是QQ邮箱，用户名部分必须是纯数字
    if strings.HasSuffix(strings.ToLower(user.Email), "@qq.com") {
        emailParts := strings.Split(user.Email, "@")
        if len(emailParts) == 2 {
            username := emailParts[0]
            // 检查用户名是否为纯数字
            isNumeric := true
            for _, char := range username {
                if char < '0' || char > '9' {
                    isNumeric = false
                    break
                }
            }
            if !isNumeric || username == "" {
                return "QQ邮箱仅支持纯数字账号（例如：123456789@qq.com）"
            }
        }
    }
    ```
  - **安全保障**：即使前端验证被绕过，后端仍能拦截无效格式

- **错误提示**：
  - 中文提示："QQ邮箱仅支持纯数字账号（例如：123456789@qq.com）"
  - 提供示例格式，帮助用户理解正确的邮箱格式

- **应用场景**：
  - 防止用户错误填写QQ邮箱格式（QQ邮箱账号只能是QQ号，即纯数字）
  - 避免因邮箱格式错误导致无法接收验证码邮件
  - 提升注册流程的用户体验和成功率

- **技术细节**：
  - 不区分大小写：`@qq.com` 和 `@QQ.COM` 都会触发验证
  - 使用正则表达式 `/^\d+$/` 确保高效准确的纯数字验证
  - 前后端使用相同的验证规则，保持一致性

---

### 22. 兑换码功能浏览器兼容性修复 ⭐ NEW
修复了壹钱包等平台兑换码在特定浏览器或特定情况下无法兑换的多个问题，全面提升兑换码功能的稳定性和用户体验。

#### 问题1: 复制兑换码带空格导致兑换失败
- **问题描述**：从壹钱包等平台复制兑换码时，经常会不小心复制到前后的空格、换行符或其他不可见字符，导致数据库查询失败，提示"无效的兑换码"
- **用户反馈**："明明是对的兑换码，但是提示无效，刷新或换个浏览器再试就好了"
- **前端修复**：`web/src/components/topup/index.jsx` (102-110行)
  ```javascript
  // 去除前后空格、换行符等不可见字符
  const trimmedCode = redemptionCode.trim();
  if (trimmedCode === '') {
    showInfo(t('请输入兑换码！'));
    return;
  }
  // 发送清理后的兑换码
  const res = await API.post('/api/user/topup', {
    key: trimmedCode,
  });
  ```
- **后端修复**：`model/redemption.go` (175-176行)
  ```go
  // 去除前后空格、换行符等不可见字符,提高兑换码容错性
  key = strings.TrimSpace(key)
  ```
- **修复效果**：
  - ✅ 兑换码前面有空格: `" ABC123"` → 正常兑换
  - ✅ 兑换码后面有空格: `"ABC123 "` → 正常兑换
  - ✅ 兑换码带换行符: `"ABC123\n"` → 正常兑换
  - ✅ 兑换码带制表符: `"ABC123\t"` → 正常兑换

#### 问题2: 快速点击或网络慢导致重复提交
- **问题描述**：用户在网络慢或误操作时快速点击"兑换"按钮，前端发送多个相同的兑换请求，第一个请求成功后，第二个请求会提示"该兑换码已被使用"
- **用户反馈**："有时候点一次说已经被使用，但刷新后重试就成功了"
- **修复方案**：`web/src/components/topup/index.jsx` (108-111行)
  ```javascript
  // 防止重复提交
  if (isSubmitting) {
    return;
  }
  setIsSubmitting(true);
  ```
- **修复效果**：
  - ✅ 按钮在提交时自动禁用（`loading={isSubmitting}`）
  - ✅ 即使快速双击，也只会发送一次请求
  - ✅ 避免并发竞态导致的"已被使用"误报

#### 问题3: 请求头类型不兼容某些浏览器
- **问题描述**：API请求头 `New-API-User` 的值为数字 `-1`，某些严格的浏览器（如特定版本的Firefox/Safari）要求请求头必须是字符串，导致请求失败
- **修复方案**：`web/src/helpers/utils.jsx` (65-76行)
  ```javascript
  export function getUserIdFromLocalStorage() {
    try {
      let user = localStorage.getItem('user');
      if (!user) return '-1';  // 字符串而非数字
      user = JSON.parse(user);
      // 确保返回字符串类型以提高浏览器兼容性
      return user.id ? String(user.id) : '-1';
    } catch (error) {
      console.warn('Failed to get user ID from localStorage:', error);
      return '-1';
    }
  }
  ```
- **修复效果**：
  - ✅ 所有浏览器都接收到统一的字符串格式请求头
  - ✅ localStorage数据异常时不会崩溃
  - ✅ 兼容Chrome、Firefox、Safari、Edge所有主流浏览器

#### 问题4: 错误处理空指针导致某些浏览器崩溃
- **问题描述**：网络错误时 `error.response` 可能为 `undefined`，直接访问 `error.response.status` 会导致某些浏览器抛出异常并停止执行
- **修复方案**：`web/src/helpers/utils.jsx` (136-161行)
  ```javascript
  // 检查 error.response 是否存在,避免空指针异常
  if (error.response && error.response.status) {
    switch (error.response.status) {
      case 401:
        // ... 处理各种HTTP状态码
    }
    return;
  }
  // 如果没有 response (网络错误等情况)
  Toast.error('错误：' + (error.message || '网络请求失败'));
  ```
- **修复效果**：
  - ✅ 网络错误时不会因空指针而崩溃
  - ✅ 显示友好的错误提示而非白屏
  - ✅ 提高在不稳定网络环境下的稳定性

#### 技术亮点
- **前后端双重防护**：前端和后端都进行trim处理，确保任一环节都能容错
- **防抖机制**：利用React state (`isSubmitting`) 实现简洁高效的防重复提交
- **类型安全**：确保所有跨浏览器的数据类型一致性
- **异常处理**：全面的try-catch和空值检查，避免意外崩溃

#### 应用场景
- 从第三方平台（壹钱包、卡密商城等）复制兑换码时容易带空格
- 移动端用户在网络波动时可能误触多次
- 不同浏览器对HTTP请求头和错误处理的严格程度不同
- 提升整体兑换码使用体验，减少用户投诉和支持成本

#### 测试建议
1. **空格测试**：复制 `" ABC123 "` (前后有空格) 进行兑换
2. **重复提交测试**：快速双击兑换按钮，检查是否只发送一次请求
3. **浏览器兼容测试**：在Chrome、Firefox、Safari、Edge分别测试
4. **网络异常测试**：开启开发者工具，限速到Slow 3G，测试错误提示

---

**分支名称:** lzh
**最后更新:** 2025-01-23
